---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "In module hierarchy"
    paths:
      - "%{facts.os.family}.yaml_%{facts.os.name}_%{facts.os.release.major}.yaml"
      - "%{facts.os.family}.yaml_%{facts.os.release.major}.yaml"
      - "%{facts.os.family}_%{facts.os.name}.yaml"
      - "%{facts.os.family}.yaml"
      - "common.yaml"
